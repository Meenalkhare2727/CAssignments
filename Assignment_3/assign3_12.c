#include<stdio.h>
int main(){

int base,index,pow;
printf("Enter base:");
scanf("%d",&base);

printf("Enter index:");
scanf("%d",&index);
pow = base^index;
printf("%d ^ %d = %d",base,index,pow);
for(int i=1;i<=base;i++)
{
for(int j=0;i<=index;i++);
{

  pow = base^index;
 
return pow;
}



}


return 0;
}
